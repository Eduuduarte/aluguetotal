import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles({
    title: {
        fontSize: '24px',
        fontWeight: 700,
        color: '#000'
    },
    subTitle: {
        fontSize: '16px',
        fontWeight: 400,
        color: '#000'
    }
})